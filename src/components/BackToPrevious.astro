---
type Props = {
  href: string;
};

const { href } = Astro.props;
---

<a
  href={href}
  class="not-prose inline-flex items-center gap-1.5 text-xs font-mono text-text-muted hover:text-accent transition-colors duration-200"
>
  <span>&larr;</span>
  <slot />
</a>
